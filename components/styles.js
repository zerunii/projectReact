import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        flex:1,
        alignItems:'center',
        justifyContent:'center',
    },
    textTopStyle:{
        fontSize:25,
        textAlign:'center',
        marginBottom:16,
    },
    textBottomStyle:{
        fontSize:16,
        textAlign:'center',
        color:'pink'
    },
});

export{styles}